import { Canvas, Meta, ArgTypes, Story, Controls, Source } from '@storybook/blocks';
import RadioStories, { Component } from './radio.stories';

<Meta title="Radio" of={RadioStories} />

# Radio

<Canvas>
  <Story of={Component} />
</Canvas>

<Controls of={Component} />

## 概要
一覧で表示された複数の選択肢から1つだけを選択できるコンポーネントです。

## 設計の原則（Do）

### 選択可能なすべての選択肢を表示します
選択肢が多く、折りたたむほうが最適な場合は、Selectを使用してください。

### できるだけ、縦に選択肢を並べます
1行に選択肢1つが望ましいです。もし複数の選択肢を水平に並べなければいけない場合は、どのラベルがどのチェックボックス / ラジオボタンのものなのかが明確になるよう配置してください。

### ラジオボタンと一緒にラベル自体もクリック可能にします
ユーザーが素早くクリックできるように、ラベル自体もクリック可能にします。視覚的に見てクリックできると判断できるエリアから数ピクセル外側までクリックできる場所が大きくなっている場合、ユーザによるエラーが大幅に減ることがわかっています。

### それぞれの選択肢にはラベルを付与します
選択状態を視覚的に分かりやすく表現します。

## 推奨しないこと（Don't）

### 複数回答できるような質問へは使用しない
複数の回答を選択させる場合は、CheckBoxを使います。
![推奨しないこと その1](packages/component-ui/images/radio_01.jpg)

### 二者択一ではっきり示すことのできる場合は使用しない
考えられるオプションが2つのみで、それらを1つの二者択一ではっきり示すことのできる場合(「オン/オフ」や「はい/いいえ」など)は、それらを1つのCheckBox、または、Toggleにまとめます。
![推奨しないこと その2](packages/component-ui/images/radio_02.jpg)

### ネスティングは避ける
他のRadioButtonあるいはCheckBoxにおいて、RadioButtonのネスティングを避けましょう。混乱を避けるためには、全選択肢を同レベルに保たなければなりません。
![推奨しないこと その3](packages/component-ui/images/radio_03.jpg)

### ラジオボタン内に操作可能な要素を含めない
ラジオボタン内にはリンクやInputなど、ユーザーが値を選択する以外の操作可能な要素は配置しないでください。これらを配置してしまうと、ユーザーがフォーム入力するための操作難易度が上がり、誤操作などにつながります。