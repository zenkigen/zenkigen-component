import { Canvas, Meta, ArgTypes, Story, Controls, Source } from '@storybook/blocks';
import ButtonStories, { Component, Base } from './button.stories';

<Meta title="Buttonx" of={ButtonStories} />

# Button

ボタンコンポーネントは、ユーザーのアクションを促すためのUIコンポーネントです。
サイズ、スタイルバリエーション、およびアイコンの配置などをカスタマイズできます。

<Canvas>
  <Story of={Component} />
</Canvas>

<Controls of={Component} />

## 🛠️ 設計の原則

### 関連するボタンやコンポーネントはグルーピング(並列)してください

関連するボタンやコンポーネントはグルーピング(並列)してください。

### 破壊的なボタンを区別します

警告ボタンがコンテンツの削除などの破壊的なアクションをもたらす場合は、破壊的なボタンスタイルを指定して、ユーザーがそれを区別できるようにします。

## ✋ 推奨しないこと

### 一つの画面にプライマリボタンを多用しない

プライマリボタンは、ページ内で最も重要なアクション事に使用します。(ex : 「同意」、「送信」、「応募」)

### 類似した表現を減らす

ラベルの目的は、ユーザーがボタンを押したときにシステムが実行する動作を明確に伝えることです。ユーザーがラベルの内容に疑問がある場合は、アプリの信頼を失う、間違ったボタンを押されてしまう、あるいはボタンがまったく押されなくなることにつながってしまいます。ラベルに同じような言葉や含みを持たせないようにし、常に文脈を念頭に置いてください。言葉の類似性が低いほど、明瞭さが増します。これは並列している、ボタンすべてに適用されます。

---

# 見出し1

これは一般的な **Markdown** のサンプルです。_イタリック_、**太字**、などの書式が利用できます。

---

## 見出し2

### リストの例

- 順不同リストアイテム1
- 順不同リストアイテム2
  - サブアイテム
- 順不同リストアイテム3

1. 順序付きリストアイテム1
2. 順序付きリストアイテム2
3. 順序付きリストアイテム3

### コードの例

インラインコード: `console.log("Hello, Markdown!");`

```javascript
// これはコードブロックの例です
function greet() {
  console.log('Hello, Markdown!');
}
greet();
```

### 引用

> これは引用の例です。  
> 複数行にわたる引用も可能です。

### リンクと画像

- [ZENKIGEN](https://zenkigen.co.jp/)

![サンプル画像](https://placehold.jp/150x150.png)

### 脚注

ここに脚注の例を示します。これは脚注です[^1].

[^1]: これは脚注の定義です。
