import { Canvas, Meta, Story, Controls, Source } from '@storybook/blocks';
import TabStories, { Component, LayoutAutoExample, LayoutEqualExample, LayoutExamples } from './tab.stories';

<Meta of={TabStories} />

# Tab

<Canvas />

<Controls of={Component} />

### Code Example

- 各Tab.Itemには一意の`id`を指定すること。

```tsx
import { Tab } from '@zenkigen-inc/component-ui';
import { useState } from 'react';

const [selectedTab, setSelectedTab] = useState('tab1');

return (
  <Tab>
    <Tab.Item id="tab1" isSelected={selectedTab === 'tab1'} onClick={setSelectedTab}>
      短い
    </Tab.Item>
    <Tab.Item id="tab2" isSelected={selectedTab === 'tab2'} onClick={setSelectedTab}>
      中程度のタブ
    </Tab.Item>
    <Tab.Item id="tab3" isSelected={selectedTab === 'tab3'} onClick={setSelectedTab} isDisabled>
      無効なタブ
    </Tab.Item>
  </Tab>
);
```

## Stories

<Canvas of={LayoutAutoExample} />
<Canvas of={LayoutEqualExample} />

## Layout examples

<Canvas of={LayoutExamples} />
