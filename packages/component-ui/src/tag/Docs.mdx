import { Canvas, Meta, ArgTypes, Story, Controls, Source } from '@storybook/addon-docs/blocks';
import TagStories, { Component } from './tag.stories';

<Meta title="Tag" of={TagStories} />

# Tag

<Canvas>
  <Story of={Component} />
</Canvas>

<Controls of={Component} />

## 概要

アイテムを説明するキーワードを使用してラベル付け、分類、または整理することができます。

## 設計の原則（Do）

### 順番はありません

タグの内容は基本的に並列となるため、掲載する順番を設ける必要はありません。

## 推奨しないこと（Don’t）

### 長すぎるラベルを設定しない

ユーザーが全体を把握しやすいように短いラベルを使用します。
